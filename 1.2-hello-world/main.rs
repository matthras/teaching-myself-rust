// main() function is similar in concept to the main() function in C++, Python, etc. - it's meant to be the very first function ran in the program

fn main() {
  println!("Hello World!");
}

// Compile by writing `rustc <name of rust file>`, then `./main' on WSL